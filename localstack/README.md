## DevOps / Localstack

### Commands:

```sh
brew install localstack/tap/localstack-cli
brew install awscli-local
brew install terraform-local

localstack --help
localstack start -d
localstack stop
```
